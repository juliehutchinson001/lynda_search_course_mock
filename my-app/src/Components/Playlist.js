import React from 'react';

const Playlists = () => (

    <aside id="#playlists" className="playlist" >
        <h1 className="playlist playlist__header" >Playlists</h1>
        <a className="playlist playlist__link" href="#playlists" > Show All </a>
        <section className="playlist playlist__list" >
            <nav>Here goes each one of my playlists</nav>        
        </section>
    </aside>

);

export default Playlists;
